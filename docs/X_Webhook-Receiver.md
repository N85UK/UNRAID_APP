# X Webhook Receiver — Universal Webhook Processing

X Webhook Receiver accepts webhooks from any JSON source and provides field mapping, security options (HMAC, bearer) and flexible storage. Works with UniFi Protect, UCG Max, Uptime Robot, GitHub, Prometheus, and custom apps.

## Key features
- Universal JSON parsing and intelligent field mapping
- Flexible authentication (HMAC, bearer, JWT)
- Rate limiting and idempotency support
- Web dashboard for search and export

Requirements: UNRAID with optional external DB. For full setup and integration details, see the main repository documentation.
